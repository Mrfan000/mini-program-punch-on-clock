# 通用打卡小程序 —— 实现方案

> 版本：v1.0
> 日期：2026-09-14
> 定位：可配置多打卡任务、个人 + 团队均可使用的通用打卡平台

---

## 一、项目概述

### 1.1 产品定位

一个「**任务可配置、个人可独立使用、团队可协作**」的通用打卡小程序。

- **个人场景**：早起、读书、健身、背单词……用户自建任务，每日打卡，看连续天数与日历。
- **团队场景**：跑团、读书会、班级、门店……创建圈子，下发打卡任务，成员互相监督，看排行与统计。
- **平台场景**：运营方可配置官方模板任务（如「21 天阅读挑战」），供用户一键加入。

### 1.2 核心价值

| 角色 | 价值 |
| --- | --- |
| 个人用户 | 低门槛建立习惯，可视化坚持成果，获得正反馈 |
| 团队组织者 | 一键下发任务、实时掌握成员进度、自动统计 |
| 平台运营 | 模板任务沉淀、内容可管控、数据可分析 |

### 1.3 设计原则

1. **打卡任务配置化**：频率、时间窗、定位、内容要求全部由任务配置驱动，不改代码即可新增玩法。
2. **个人与团队统一模型**：个人任务 = 只有自己的「圈子」，共用同一套数据模型与代码。
3. **云原生**：充分利用微信云开发，免运维、免鉴权，快速上线。
4. **可演进**：一期做扎实核心闭环，二期扩展成就体系、AI 报告等。

---

## 二、技术选型

### 2.1 总体技术栈

| 层次 | 选型 | 说明 |
| --- | --- | --- |
| 小程序端 | **微信原生小程序 + TypeScript** | 与云开发能力最贴合，性能最优 |
| UI 组件 | **TDesign 小程序组件库** | 腾讯出品，与微信生态一致，组件丰富 |
| 状态管理 | 内置轻量 Store（模块级单例 + 订阅） | 零第三方依赖，`app` 启动链路不受 npm 构建影响；后续如需更强响应式可按需接入 `mobx-miniprogram` |
| 后端 | **微信云开发** | 云函数 + 云数据库 + 云存储 |
| 管理后台 | **Vue3 + Vite + Element Plus** | 通用 Web 技术栈，开发效率高 |
| 后台接入 | **@cloudbase/js-sdk** | 复用同一套云环境与云函数 |
| 后台部署 | **云开发静态网站托管** | 与管理端同环境，免额外服务器 |

### 2.2 为什么前端推荐「微信原生小程序」

在**已确定后端为微信云开发**的前提下，原生小程序是最优解：

1. **云能力直连**：`wx.cloud.callFunction` / `wx.cloud.database` 开箱即用，无需封装登录态，`openid` 云函数自动注入。
2. **原生能力最全**：订阅消息、分享、地理位置、相机/录音、`wx.getUserProfile` 等能力调用路径最短。
3. **性能与包体**：无跨端运行时开销，首屏与分包控制更精细。
4. **生态一致**：TDesign 小程序版与微信设计规范一致，视觉与体验统一。
5. **团队成本**：若后续确实需要多端，可平滑迁移到 Taro/uni-app，核心业务逻辑已抽到云函数中。

> **选型定稿（已确认）**：项目当前**无多端计划**；若未来确实需要覆盖支付宝/抖音/App 等平台，将**自建服务器与后端接口并重构**，而非依赖跨端框架。因此前端**确定采用微信原生小程序**，不引入 Taro/uni-app。

> 跨端方案对比（备查）：Taro (React + TypeScript) 可一套代码多端且支持云开发，uni-app 生态更广。但在无多端需求时，其抽象层会带来额外调试成本与包体开销，原生性价比最高。

### 2.3 为什么用「云开发」

- 无需自建服务器、域名备案、HTTPS 证书、登录态体系，**开发速度最快**。
- 云函数天然获得可信 `openid`，权限校验在服务端完成更安全。
- 云数据库自带索引、聚合、事务（部分）、权限规则，满足打卡统计需求。
- 与微信订阅消息、内容安全接口（`msgSecCheck` 等）在同一体系内调用顺畅。
- 成本按量计费，早期几乎为 0。

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────┐
│                      微信小程序端 (用户)                    │
│   WXML/WXSS/TS · TDesign · mobx-miniprogram               │
└───────────────┬──────────────────────────────────────────┘
                │ wx.cloud.callFunction / database(只读)
                ▼
┌──────────────────────────────────────────────────────────┐
│                   微信云开发 (CloudBase)                   │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │  云函数层 (业务逻辑唯一入口，纯服务端)                  │  │
│  │  user / task / checkin / group / interaction /      │  │
│  │  makeup / ranking / notify / admin / common(layer)  │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐  │
│  │ 云数据库      │ │ 云存储        │ │ 定时触发器        │  │
│  │ (集合+索引)   │ │ (图片/音视频) │ │ (提醒/统计/结算)  │  │
│  └──────────────┘ └──────────────┘ └──────────────────┘  │
└───────────────┬──────────────────────────────────────────┘
                │ @cloudbase/js-sdk (Web 端登录 + 调云函数)
                ▼
┌──────────────────────────────────────────────────────────┐
│             管理后台 Web (Vue3 + Element Plus)             │
│   任务/圈子管理 · 统计报表 · 数据导出 · 审核 · 系统配置       │
│   —— 部署于「云开发静态网站托管」                          │
└──────────────────────────────────────────────────────────┘

外部能力：订阅消息推送 · 内容安全(msgSecCheck/mediaCheckAsync) · 微信支付(二期)
```

### 3.1 分层职责

- **小程序端**：只做展示与交互，**不直接写数据库**（复杂写操作全部走云函数），读操作可按权限直连数据库以降低延迟。
- **云函数层**：所有业务写入、校验、统计、鉴权的唯一权威入口。
- **云数据库**：存储所有业务数据，客户端直连需配置严格安全规则。
- **定时触发器**：打卡提醒、每日排行榜结算、连续天数断签检测、统计汇总。
- **管理后台**：管理员配置与运营，复用同一云环境与云函数（`admin` 函数做角色校验）。

---

## 四、功能模块设计

### 4.1 用户端功能

#### 4.1.1 用户与登录

- 微信一键登录（云函数获取 `openid`，首次自动建用户）
- 头像昵称设置（微信头像昵称填写能力）
- 个人主页：我的任务、打卡日历、统计、成就
- 消息中心：打卡提醒、审批通知、互动消息

#### 4.1.2 任务（打卡项）管理

- **创建任务**：标题、描述、封面/图标、分类
- **打卡频率**：
  - 每日 / 每周 N 次 / 每月 N 次 / 指定星期几 / 自定义周期
- **时间窗口**：如 05:00–09:00 之间打卡有效
- **定位要求**：指定地点 + 半径（可选，需申请 `getLocation` 权限）
- **内容要求**：图片 / 文字 / 语音 / 视频，至少填几项
- **提醒设置**：开启后到点推送订阅消息
- **有效期**：开始日期 ~ 结束日期
- **可见性**：公开（广场）/ 圈子内 / 仅自己
- **任务广场**：浏览公开任务/官方模板，一键加入

#### 4.1.3 打卡（核心闭环）

- 任务详情页 → 打卡按钮 → 填写内容（图片/文字/定位）→ 提交
- **服务端校验**：时间窗合法性、频率是否超额、定位是否在范围内、当日是否已打卡（幂等去重）、内容安全
- 打卡成功 → 更新连续天数/统计 → 触发互动（可选分享到圈子）
- **打卡记录**：我的打卡历史、按任务筛选、月历视图
- **补卡申请**：漏卡可发起补卡，需理由（团队任务需管理员审批）
- **打卡详情**：内容、时间、地点、点赞评论

#### 4.1.4 圈子 / 团队

- 创建圈子（头像、名称、简介、加入方式）
- 加入方式：公开加入 / 申请审批 / 邀请链接
- 成员角色：圈主 / 管理员 / 成员
- 圈子任务：圈主下发任务，成员参与
- 圈子动态：成员打卡流、点赞评论
- 成员管理：审批、移除、设置管理员
- 圈子排行：今日榜 / 周榜 / 总榜（按打卡数、连续天数）

#### 4.1.5 统计与激励

- **个人统计**：总打卡、连续天数、最长连续、本月完成率、日历热力图
- **排行榜**：任务榜 / 圈子榜，支持日/周/月/总
- **成就徽章**（二期）：连续 7 天、100 次打卡、早起达人等
- **分享**：生成打卡海报，分享到聊天/朋友圈

#### 4.1.6 互动

- 点赞、评论、回复
- 评论内容安全校验
- 消息通知（被点赞/评论/审批结果）

### 4.2 管理后台功能

| 模块 | 功能 |
| --- | --- |
| 数据看板 | 用户数、活跃、打卡总量、任务数、趋势图 |
| 用户管理 | 查询、封禁/解封、详情、打卡记录查看 |
| 任务管理 | 列表、审核、下架、官方模板配置 |
| 圈子管理 | 列表、解散、成员查看、敏感圈子处理 |
| 内容审核 | 打卡内容/评论的举报处理、批量下架 |
| 补卡审批 | 跨圈子的补卡申请处理（可选） |
| 报表统计 | 任务完成率、留存、导出 Excel |
| 系统配置 | 分类字典、敏感词、订阅消息模板、首页轮播 |
| 操作日志 | 管理员操作审计 |

---

## 五、数据库设计（云数据库集合）

> 命名规范：小写下划线；所有集合统一含 `createdAt` / `updatedAt`（毫秒时间戳）；软删除用 `isDeleted`。

### 5.1 users（用户）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| _id | string | 主键 |
| _openid | string | 微信 openid（云开发自动写入） |
| unionid | string | 可选，多应用打通 |
| nickName | string | 昵称 |
| avatarUrl | string | 头像 |
| phone | string | 手机号（可选） |
| role | string | `user` / `admin`（后台权限） |
| status | string | `normal` / `banned` |
| registerAt | number | 注册时间 |

**索引**：`_openid`(唯一)
**权限**：仅创建者可读写；`role` 更新仅云函数可操作

### 5.2 tasks（打卡任务）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| _id | string | 主键 |
| groupId | string \| null | 所属圈子；`null` = 个人任务 |
| creatorId | string | 创建者 userId |
| title / desc | string | 标题 / 描述 |
| cover / icon | string | 封面图 / 图标（云存储 fileID） |
| categoryId | string | 分类 |
| frequency | object | 频率配置，见下 |
| timeWindows | array | `[{start:"05:00", end:"09:00"}]` |
| requireLocation | bool | 是否需要定位 |
| location | object | `{latitude, longitude, radius, address}` |
| contentRule | object | `{needImage, minImage, needText, minText, allowAudio, allowVideo}` |
| remindEnabled / remindTime | bool / string | 提醒 |
| startDate / endDate | string | 有效期 `YYYY-MM-DD` |
| visibility | string | `public` / `group` / `private` |
| isTemplate | bool | 是否官方模板 |
| status | string | `draft` / `published` / `paused` / `archived` |
| participantCount / checkinCount | number | 冗余统计 |

**`frequency` 结构**

```json
{
  "type": "daily | weekly | monthly | custom",
  "weekDays": [1,2,3,4,5],       // 指定星期（1=周一）
  "timesPerPeriod": 3,            // 每周/每月次数
  "periodKey": "day | week | month" // 用于去重与统计的粒度
}
```

**索引**：`(groupId, status)`、`(creatorId)`、`(visibility, status, isTemplate)`

### 5.3 group_members（圈子成员）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| groupId / userId | string | 关联 |
| role | string | `owner` / `admin` / `member` |
| joinAt | number | 加入时间 |
| status | string | `normal` / `removed` / `pending` |

**索引**：`(groupId, userId)` 唯一；`(userId)`

### 5.4 groups（圈子）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| name / cover / desc | string | 基本信息 |
| ownerId | string | 圈主 |
| joinMode | string | `public` / `approval` / `invite` |
| memberCount / taskCount | number | 冗余统计 |
| status | string | `normal` / `banned` / `closed` |

**索引**：`(ownerId)`、`(status)`

### 5.5 checkin_records（打卡记录）—— 核心表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| taskId / userId / groupId | string | 关联 |
| periodKey | string | 去重键，如 `2026-09-14` / `2026-W37` |
| checkinTime | number | 打卡时间戳 |
| location | object | 定位快照 |
| content | object | `{images:[], text, audios:[], videos:[]}` |
| status | string | `normal` / `late` / `makeup` |
| isMakeup | bool | 是否补卡 |
| streakAtTime | number | 打卡时的连续天数快照 |
| likeCount / commentCount | number | 冗余计数 |

**索引**：
- `(taskId, userId, periodKey)` **唯一** —— 幂等去重
- `(taskId, checkinTime)` —— 排行/动态流
- `(userId, checkinTime)` —— 我的记录
- `(groupId, checkinTime)` —— 圈子动态

### 5.6 user_task_stats（用户-任务统计）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| taskId / userId | string | 关联 |
| totalCount | number | 累计打卡 |
| currentStreak | number | 当前连续 |
| maxStreak | number | 最长连续 |
| lastPeriodKey | string | 上次打卡周期 |
| monthCount | number | 本月次数 |

**索引**：`(taskId, userId)` 唯一；`(taskId, currentStreak desc)`、`(taskId, monthCount desc)` 用于排行

### 5.7 makeup_requests（补卡申请）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| taskId / userId / periodKey | string | 补哪天 |
| reason / evidence | string / array | 理由 / 凭证 |
| status | string | `pending` / `approved` / `rejected` |
| approverId / approveAt / approveRemark | - | 审批信息 |

### 5.8 interactions（互动）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| recordId / taskId / userId | string | 关联 |
| type | string | `like` / `comment` |
| content | string | 评论内容 |
| replyTo | string | 回复目标评论 |
| status | string | `normal` / `hidden` |

**索引**：`(recordId, type)`、`(userId, recordId, type)` 唯一（防重复点赞）

### 5.9 其他集合

- `categories` 任务分类字典
- `notify_subscriptions` 订阅授权记录（模板 ID、剩余次数）
- `messages` 站内消息
- `reports` 举报
- `settings` 系统配置（KV）
- `admin_logs` 管理操作日志
- `stat_daily` 每日汇总（供看板快速查询）

---

## 六、云函数设计

> **设计策略**：按领域拆分少量函数，函数内用 `event.action` 路由，减少冷启动与部署成本；公共代码放入 `common` 层（Layer）复用。

### 6.1 函数清单

| 函数名 | 职责 | 主要 action |
| --- | --- | --- |
| `user` | 登录、资料、消息 | `login` `updateProfile` `getProfile` `messages` |
| `task` | 任务 CRUD、加入 | `create` `update` `list` `detail` `publish` `join` `quit` `members` `square` |
| `checkin` | 打卡核心 | `submit` `list` `detail` `calendar` `delete` `stats` |
| `group` | 圈子 | `create` `update` `list` `detail` `join` `approve` `members` `leave` `remove` |
| `interaction` | 互动 | `like` `comment` `list` `delete` |
| `makeup` | 补卡 | `apply` `approve` `list` |
| `ranking` | 排行 | `taskRank` `groupRank` `userStat` |
| `notify` | 通知 | `subscribe` `send`（供内部/定时器调用） |
| `admin` | 后台专用（校验 role） | `dashboard` `userList` `taskAudit` `groupList` `stats` `export` `config` |
| `common` | 公共层（非函数） | 鉴权、响应封装、时间工具、内容安全、统计更新 |

### 6.2 核心接口：`checkin.submit` 逻辑

```
1. 参数校验（taskId、content、location）
2. 查任务，校验 status = published、在有效期内
3. 计算 periodKey（按 frequency 粒度）
4. 校验频率是否超额（weekly/monthly N 次）
5. 校验时间窗口 timeWindows 内（不在窗口 → 标记 late 或拒绝，可配置）
6. 校验定位（requireLocation 时计算球面距离 ≤ radius，否则拒绝）
7. 内容安全校验（文本 msgSecCheck / 图片 imgSecCheck）
8. 幂等写入 checkin_records（唯一索引兜底）—— 事务内
9. 更新 user_task_stats（连续天数、累计、本月）
10. 更新 tasks 冗余计数
11. 异步触发通知/动态/排行缓存刷新
12. 返回打卡结果 + 当前连续天数
```

### 6.3 定时触发器

| 触发器 | 频率 | 任务 |
| --- | --- | --- |
| `remindJob` | 每 5 分钟 | 扫描 `remindTime` 到点的任务，向已订阅用户推订阅消息 |
| `streakJob` | 每日 00:10 | 检测昨日/本周期未打卡用户，重置 `currentStreak` |
| `statJob` | 每日 01:00 | 汇总 `stat_daily`，刷新排行榜缓存 |
| `expireJob` | 每日 02:00 | 归档到期任务 |

---

## 七、关键业务流程

### 7.1 打卡主流程

```
用户打开任务 → 点击「打卡」
   → 填写内容（图片/文字）+ 授权定位
   → 上传文件到云存储，得到 fileID
   → 调用 checkin.submit
   → 云函数全套校验（时间窗/频率/定位/去重/内容安全）
   → 写入记录 + 更新统计 → 返回连续天数
   → 前端展示成功动画 + 可分享卡片
```

### 7.2 圈子任务流程

```
圈主创建圈子 → 邀请/审批成员
   → 创建任务（设置 frequency / 定位 / 提醒）
   → 成员在任务广场或圈子内加入任务
   → 成员每日打卡 → 圈子动态流展示
   → 排行实时更新 → 圈主查看完成率统计
```

### 7.3 补卡审批流程

```
成员发起补卡（选周期 + 理由 + 凭证）
   → 生成 makeup_requests(pending)
   → 圈主/管理员收到通知
   → 审批通过 → 创建 checkin_records(status=makeup) + 重算统计
   → 通知申请人
```

### 7.4 订阅消息流程

```
用户开启任务提醒 → 前端 wx.requestSubscribeMessage 获得授权
   → 授权记录写入 notify_subscriptions
   → 定时器 remindJob 到点 → notify.send 推送
   → 前端记录剩余授权次数（一次性订阅消耗后需再次授权）
```

---

## 八、目录结构

### 8.1 小程序端

```
miniprogram/
├── app.ts / app.json / app.wxss
├── pages/
│   ├── index/            # 首页：我的任务 + 今日待打卡
│   ├── square/           # 任务广场
│   ├── task/
│   │   ├── detail/       # 任务详情 + 打卡入口
│   │   ├── create/       # 创建/编辑任务
│   │   └── checkin/      # 打卡内容填写
│   ├── record/           # 打卡记录 + 月历
│   ├── group/
│   │   ├── list/ detail/ create/ members/ feed/
│   ├── ranking/          # 排行榜
│   ├── mine/             # 个人中心 + 统计
│   ├── message/          # 消息中心
│   └── makeup/           # 补卡
├── components/           # 通用组件（任务卡片/日历/打卡按钮/上传等）
├── utils/                # 请求封装、日期、定位、格式化
├── store/                # mobx 全局状态
├── images/               # 静态资源
└── cloudfunctions/       # 云函数（见 6.1）
```

### 8.2 管理后台

```
admin-web/
├── index.html
├── vite.config.ts
├── src/
│   ├── main.ts / App.vue
│   ├── router/           # 路由 + 权限守卫
│   ├── store/            # Pinia
│   ├── api/              # 云函数调用封装
│   ├── layouts/          # 侧边栏布局
│   ├── views/
│   │   ├── dashboard/    # 数据看板
│   │   ├── user/ task/ group/ content/
│   │   ├── makeup/ report/ config/ log/
│   └── components/
└── cloudbaserc.json      # 静态托管配置
```

---

## 九、权限与角色模型

| 角色 | 权限范围 |
| --- | --- |
| 游客 | 浏览任务广场（只读） |
| 普通用户 | 创建个人任务、打卡、加入圈子、互动 |
| 圈子成员 | 参与圈子任务、圈子动态、圈子排行 |
| 圈子管理员 | 审批入圈/补卡、管理任务、移除成员 |
| 圈主 | 圈子全部权限、转让、解散 |
| 平台管理员 | 后台全部管理权限 |

**实现方式**：

1. **数据库安全规则**：客户端读操作按 `_openid` 或公开字段限制；写操作一律禁止客户端直连。
2. **云函数鉴权**：`common` 层统一从 `cloud.getWXContext()` 取 `openid` → 查 `users` → 校验角色/归属。
3. **后台登录**：Web 端使用云开发登录（自定义登录/匿名）+ 管理员白名单（`users.role = admin`），`admin` 云函数二次校验。

---

## 十、非功能性需求

### 10.1 安全

- 所有写操作走云函数，禁止客户端直接写库
- 内容安全：文本 `security.msgSecCheck`、图片 `imgSecCheck`、音视频 `mediaCheckAsync`
- 防刷：打卡接口加频率限制（单用户单任务单周期幂等 + 云函数并发限流）
- 敏感词过滤、举报机制、黑名单

### 10.2 合规（上线必查）

- 配置《小程序用户隐私保护指引》，声明收集的信息（头像、昵称、位置、相册）
- `wx.getLocation` 需在小程序后台**申请接口权限**并说明用途，否则调用失败
- 用户信息收集遵循最小必要原则，提供注销与数据删除入口
- 内容需符合平台规范，具备审核与举报能力

### 10.3 性能

- **分包加载**：主包放首页/任务/打卡核心，圈子/排行/后台等放分包
- 图片：云存储 + 图片处理（缩略图），CDN 加速，懒加载
- 列表分页（每页 20），下拉刷新 + 触底加载
- 云函数冷启动：合并调用、预热、公共层复用
- 排行/统计走冗余字段 + 定时汇总，避免实时全表聚合

### 10.4 可用性

- 打卡失败本地缓存草稿，联网后重试
- 云函数错误统一响应码，前端友好提示
- 关键操作（打卡/审批）幂等，支持重复点击

---

## 十一、里程碑规划

| 阶段 | 内容 | 交付 |
| --- | --- | --- |
| **M1 基础框架** | 项目脚手架、云环境、用户登录、UI 组件库、公共层 | 可登录、可跑通云函数 |
| **M2 任务+打卡核心** | 任务 CRUD、打卡闭环、记录与日历、个人统计 | 个人完整可用 |
| **M3 圈子+互动** | 圈子、成员、圈子任务、动态流、点赞评论、排行 | 团队可用 |
| **M4 后台+通知** | 管理后台（看板/用户/任务/圈子/审核）、订阅消息提醒、补卡审批 | 运营可用 |
| **M5 上线打磨** | 内容安全、合规、性能优化、埋点、体验细节、提审上线 | 正式发布 |

> **节奏已确认**：M1–M2 先跑通「个人打卡」最小闭环并快速验证，再迭代 M3 圈子与 M4 后台。M1–M2 期间的数据模型按最终形态设计（`groupId` 可为 `null` 即个人任务），确保 M3 无需改表。

**M1–M2 交付范围（当前迭代目标）**

| 里程碑 | 具体交付物 |
| --- | --- |
| M1 | 小程序脚手架、云环境接入、`user` 云函数与登录、`common` 公共层、TDesign 集成、请求/存储工具封装 |
| M2 | `task` 任务 CRUD、`checkin.submit` 打卡闭环、打卡记录与月历、个人统计（连续天数/累计）、任务广场（公开任务） |

---

## 十二、风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| `getLocation` 权限未申请 | 定位打卡不可用 | 提前在后台申请；提供「无需定位」的任务配置降级 |
| 订阅消息一次性限制 | 提醒到达率低 | 每次进入任务引导授权；关键提醒 + 站内消息兜底 |
| 云函数冷启动 | 打卡慢 | 公共层复用、减少依赖、必要时预热 |
| 连续天数并发写错乱 | 统计不准 | 唯一索引 + 事务 + 定时校准 `statJob` |
| 内容违规 | 审核被拒/封禁 | 全链路内容安全 + 人工审核 + 举报 |
| 免费额度超限 | 费用上升 | 监控用量、图片压缩、查询走索引、缓存聚合 |

---

## 十三、二期演进方向

- 成就徽章体系、等级与积分、打卡兑换
- AI 打卡内容点评 / 周报自动生成
- 计划商城：付费挑战营、课程打卡
- 微信支付：保证金打卡、返现激励
- 多端覆盖：**自建服务器与后端接口，前端重构**（不走 Taro/uni-app 跨端框架）
- 数据大屏、精细化运营看板

---

## 十四、关键决策记录

| 编号 | 决策项 | 结论 | 理由 |
| --- | --- | --- | --- |
| D1 | 产品定位 | 通用打卡平台，个人 + 团队均可使用 | 任务配置化，覆盖面最广 |
| D2 | 小程序端技术栈 | **微信原生小程序 + TypeScript** | 无多端需求；与云开发能力最贴合，性能最优 |
| D3 | 后端方案 | **微信云开发**（云函数 + 云数据库 + 云存储） | 免运维、免鉴权、开发最快，早期成本近零 |
| D4 | 管理后台 | 需要，**Vue3 + Element Plus + 云开发静态托管** | 复用同一云环境与云函数 |
| D5 | 多端规划 | 暂无；未来如需多端，**自建后端并重构** | 不引入跨端框架的抽象成本 |
| D6 | 迭代节奏 | **M1→M2 先跑通个人打卡最小闭环** | 快速验证，数据模型按最终形态设计避免返工 |

---

## 十五、下一步

1. **准备环境**：注册/配置微信小程序，开通云开发环境，记录 **AppID** 与 **环境 ID**
2. **搭建 M1 脚手架**：小程序项目结构 + 云开发接入 + `common` 公共层 + `user` 云函数 + TDesign 集成
3. **实现 M2 核心**：`task` 任务 CRUD + `checkin.submit` 打卡闭环 + 记录/月历 + 个人统计
4. **联调验证**：个人打卡闭环端到端跑通
5. 进入 M3 圈子与 M4 后台

> 附：开发环境要求 —— 微信开发者工具（最新稳定版）、Node.js 18+、微信小程序 AppID、云开发环境 ID。

---

## 附录 A：M1 环境搭建与部署步骤

### A.1 已完成的工程结构

```
mini-program-punch-on-clock/
├── project.config.json          # AppID / 云函数根目录 / TS 编译开关
├── package.json                 # 根脚本（sync:common）
├── scripts/sync-common.mjs      # 公共层同步脚本
├── cloudfunctions/
│   ├── common/                  # 公共层源码（唯一来源）
│   │   ├── errors.js            # 统一错误码 + BusinessError
│   │   ├── response.js          # { code, message, data } 响应封装
│   │   ├── constants.js         # 集合名 + 枚举
│   │   ├── db.js                # 数据库/指令/服务端时间
│   │   ├── date.js              # periodKey / 时间窗 / 球面距离
│   │   ├── validator.js         # 参数校验
│   │   └── auth.js              # openid 鉴权 / 用户校验 / 管理员校验
│   └── user/                    # 用户域云函数
│       ├── index.js             # action 路由
│       ├── actions/             # login / getProfile / updateProfile
│       └── lib/                 # 由 sync:common 生成（勿手改）
└── miniprogram/
    ├── app.ts / app.json / app.wxss
    ├── config/index.ts          # 云环境 ID / AppID / 云存储目录约定
    ├── typings/model.d.ts       # 全局业务模型声明
    ├── store/user.ts            # 用户全局状态（零依赖）
    ├── utils/                   # cloud / storage / date / toast / constants
    └── pages/index, pages/mine
```

### A.2 首次运行步骤

1. **导入项目**：微信开发者工具 → 导入项目，目录选 `mini-program-punch-on-clock`，AppID 填 `wx96eee2c33776978c`
2. **开通云开发**：确认环境 `864ca59f80df166ae7dabf8141d11c92` 已创建
3. **安装依赖**：`cd miniprogram && npm install`
4. **构建 npm**：开发者工具 → 工具 → 构建 npm（TDesign 组件必需）
5. **同步公共层**：项目根目录执行 `npm run sync:common`
6. **部署云函数**：右键 `cloudfunctions/user` → 上传并部署（云端安装依赖）
7. **创建集合与索引**：云开发控制台创建 `users` 集合，为 `_openid` 建唯一索引

### A.3 验收标准（M1 完成定义）

- 首页「开发自检」区正确显示 `openid` 与用户状态 → 登录链路打通
- 「我的」页可修改并保存头像昵称 → 云函数写入链路打通
- 云开发控制台 `users` 集合出现对应记录

### A.4 常见问题

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| 提示找不到 `miniprogram-api-typings` | 未安装依赖 | 在 `miniprogram/` 下执行 `npm install` |
| 页面报 `tdesign-miniprogram/...` 找不到 | 未构建 npm | 工具 → 构建 npm |
| 云函数报 `Cannot find module './lib'` | 未同步公共层 | 根目录执行 `npm run sync:common` 后重新部署 |
| 首页 openid 显示为空 | 云函数未部署 / 环境 ID 不符 | 核对 `config/index.ts` 与云开发环境 |

> `cloudfunctions/*/lib/` 为生成产物（已在 `.gitignore` 忽略）。每次修改 `cloudfunctions/common/` 后，需重新执行 `npm run sync:common` 并重新部署受影响的云函数。
